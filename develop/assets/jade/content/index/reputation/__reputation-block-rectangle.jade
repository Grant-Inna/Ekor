mixin reputation-block-rectangle( number, title, text)
   article.reputation__block-rectangle(class='reputation__block-#{number}')
      .reputation__number.text-small.text-opacity 0#{number}
      .reputation__title !{title}
      .reputation__text.text-14 !{text}
